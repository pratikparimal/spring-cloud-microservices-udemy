spring:
  application:
    name: limit-service
  cloud:
    config:
      uri:
        - http://localhost:8888
  profiles.active: dev
server:
  port: 8080
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka